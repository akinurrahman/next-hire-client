export const API_BASE_URL = process.env.NEXT_PUBLIC_BASE_API;

export const APP_NAME = "NextHire";

export const COOKIE_NAMES = {
  ACCESS_TOKEN: "access_token",
  REFRESH_TOKEN: "refresh_token",
  USER: "user",
} as const;
